# MDT 技能命令自定义

这个插件允许你通过配置文件新增技能指令，例如 `/io`、`/ki`。

支持能力：

- 自定义任意客户端技能命令
- 每个技能允许多个权限组
- 每个技能允许多个货币消耗
- 消耗数量支持固定值、参数值、余额值、随机值
- 支持在玩家脚下生成指定方块或随机方块

默认配置文件：

`config/mods/config/mdt-skill-command-customizer/skill-command-customizer.properties`

依赖：

- `mdt-player-economy-system`
